# 作業指針（AI 向け）

このリポジトリはラノベの執筆用。コードではなく文章を書く。

## 書く前に必ず読む

- `core/principles.md` — 何のために書くか。迷ったらこれが最優先
- `core/workflow.md` — 進め方
- `core/writing-style.md` — 文体の方針
- `core/naming.md` — 用語と名づけの基準
- `core/chronicle.md` — 世界の記録の取り方と、話を尽きさせない仕組み
- 対象作品の `worlds/<宇宙>/stories/<作品名>/meta.md` と `plot.md`
- その作品が載る `worlds/<宇宙>/world.md` と `worlds/<宇宙>/<星>/` の各ファイル

## 守ること

- **設定の一元管理**: 本文を書く途中で新しい設定（地名・組織・技名・過去の出来事）を作ったら、`worlds/` 側にも書き戻す。本文にしか存在しない設定を残さない。
- **1 レコード 1 ファイル**: キャラクター・物・用語・概念は、**一つにつき一ファイル**。front matter がレコード、その下が文章。雛形は `python3 tools/chronicle.py template --kind 人物`。置き場所は自由で、`記録:` がどの台帳に入るかを決める。`core/chronicle.md`。
- **`glossary.md` を手で書かない**: 用語集は記事から作る索引。語を足すときは `terms/` にファイルを作り、`chronicle.py index` で作り直す。
- **書く前に断面を取る**: その話が立つ星・年・場所を決めて `python3 tools/chronicle.py brief --star <星> --year <年> --place <場所>` を先に読む。設定ファイルを頭から読み直すより早く、取りこぼしが出ない。**本文を書くあいだは `--full` を付けない**（住人が知らないことを書いてしまう）。`core/chronicle.md`。
- **書いたら台帳に戻す**: 一話書き終えたら `worlds/<宇宙>/records/` に、起きたこと・人物の行動（動機と代償と結果）・動いた火種を追記して `chronicle.py check` を通す。**どの火種の値も動かない話は、削れる話。**
- **用語は日本語として自然に**: 読者は日本人。日本的な漢字（訓読み）・ひらがな・カタカナ英語で名づける。音読み二字熟語の造語を重ねない。`core/naming.md`。
- **なろう系テンプレを使わない**: 禁止事項の具体リストは `core/principles.md`。構造として避ける。
- **アバウトな要素は乱数で決める**: `tools/roll.py` を使い、引いた目を `worlds/<宇宙>/rolls.md` に残す。AI の第一想起で埋めない。
- **同じ宇宙の中で矛盾しない**: 暦・共通現象・星どうしの関係は星をまたいで一致させる。宇宙が違えば矛盾してよい（平行宇宙）。
- **既存設定の優先**: `worlds/` に書いてある設定と食い違う本文は書かない。変えたいときは先に設定側を直す。
- **作品は宇宙の中で完結させる**: 本文は `worlds/<宇宙>/stories/<作品名>/` に置く。**複数の宇宙をまたぐ作品は作らない。** 別の宇宙の設定を引きたくなったら、それは別の作品である。
- **1 話 1 ファイル**: `worlds/<宇宙>/stories/<作品名>/episodes/NNN.md`。既存話の書き換えを頼まれていない限り、過去話は触らない。
- **文体の一貫性**: 途中から語り口を変えない。一人称/三人称、地の文の温度、句読点の癖を作品内で揃える。
- **要約で済ませない**: 「〜という展開があった」で流さず、場面として書く。ダイジェストは読者が飛ばす。

## 作業の締め

- 作業が終わったら、**その内容でプルリクエストがなければ作成し、リンクを表示する**。
  すでにあるなら、そのブランチへプッシュしてリンクを示す
- 台帳をいじったら `python3 tools/chronicle.py check --world <宇宙>` を通してから終える

## やらないこと

- 頼まれていない話数を勝手に書き足す
- 既存の原稿を「ついでに」推敲して書き換える（指示があったときだけ）
- プロットにない大きな展開の追加（提案は歓迎、無断実装は不可）
