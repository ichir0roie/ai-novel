#!/usr/bin/env python3
"""`IHG/chronicle.md`「出来事の text は場面で書く」の要旨を、AI へのプロンプトに
埋め込める形に切り出したもの。

`IHG/chronicle.md` は人間(Claude)が `commit_event` を呼ぶときの基準を長い
散文で説明している。`DEM/local_ai/`(`ai_client` を介してローカル AI が db
への確定まで一気に行う `time_keeper` の常駐ループ)は、その基準を対話越しに
確認できないので、`naming.py` と同じやり方で要旨だけを定数として持つ。

**Claude 自身が `commit_event` の `text` を書くとき**は、この定数ではなく
`IHG/chronicle.md` を直接読む。ここに切り出した定数は、常駐ループ向けの
簡略版でしかない。

**`IHG/chronicle.md` の基準を変えたら、ここも合わせて直す。**
"""
from __future__ import annotations

EVENT_SCENE_INSTRUCTION = (
    "text は「起きたことの要約」ではない。「〜という出来事があった」の"
    "一文で済ませず、軽い小説として1000文字程度で、その時その場のキャラク"
    "ターの思考・行動・(この出来事が及ぼす)影響を場面として書く。"
)

EVENT_PROGRESSION_INSTRUCTION = (
    "直近の出来事の一覧(名前・種別)を先に見て、同じ関係者・同じ場所で、"
    "前回までと似た種別の出来事の結末(確認する・合意する、など)を、名前"
    "だけ変えてそのまま繰り返さない。前回までに積み上がった対立・決定を"
    "土台にして、状況を具体的に一歩動かす(決着する・破られる・第三者が"
    "絡む・決定的な行動に出る、など)か、関わる相手・題材を変える。"
    "本当に何も動かないなら、無理に出来事を作らず event_name を空のまま"
    "返してよい。"
)
